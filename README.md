# wix.github.io

These are the sources for [wix.github.io](http://wix.github.io) which is based on [jekyll](https://github.com/mojombo/jekyll).

## Contributing

Clone and run locally:

```
$ gem install jekyll
$ git clone git@github.com:wix/wix.github.io.git
$ cd wix.github.io/
$ jekyll serve --watch
```

Add content under `_posts`, change/add layouts under `_layout`.